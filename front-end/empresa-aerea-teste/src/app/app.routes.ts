import { Routes } from '@angular/router';
import { AutCadComponent } from './paginas/aut-cad/aut-cad.component';

export const routes: Routes = [    { path: 'cadastrar', component: AutCadComponent }, // Configura a rota padrão para o componente
];
